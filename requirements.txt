# Web front end (floorplan_v1/app.py) + the existing solver pipeline it drives.
streamlit>=1.35
anthropic>=0.34
ortools>=9.9

# The polished-render button on a result (floorplan_v1/polish.py, see
# NANO_BANANA_RENDER_DESIGN.md). These three used to be deliberately absent
# because the deployed app never called Gemini; it now does, but ONLY when
# someone presses that button — `python3 floorplan_v1/polish.py --self-check`
# proves the app has no module-level import of any of them, so a deploy that
# nobody clicks still never loads the SDK or spends a peso.
#
# cairosvg additionally needs the native cairo library, which on Streamlit
# Cloud comes from packages.txt (libcairo2) — a Python-only requirements file
# is not enough for it.
google-genai>=1.0
cairosvg>=2.7
pillow>=10.0
